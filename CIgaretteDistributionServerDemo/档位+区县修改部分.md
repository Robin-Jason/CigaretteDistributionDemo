服务层将删除旧数据改为删除对应类型的旧数据：// 只删除当前要处理的档位+区县类型卷烟的旧数据
        List<String> countyAdvCigCodes = countyAdvData.stream()
                .map(DemoTestAdvData::getCigCode)
                .collect(Collectors.toList());
        
        List<DemoTestData> oldCountyPredictions = testDataRepository.findByYearAndMonthAndWeekSeqAndCigCodeIn(year, month, weekSeq, countyAdvCigCodes);
        if (!oldCountyPredictions.isEmpty()) {
            testDataRepository.deleteAll(oldCountyPredictions);
            log.info("已删除 {} 条档位+区县类型的旧预测数据", oldCountyPredictions.size());
        }