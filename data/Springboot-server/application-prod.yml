server:
  port: 28080

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/marketing?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=GMT%2B8
    username: syyc
    password: syyc@12AB
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: false
       
logging:
  level:
    root: INFO
    org.hibernate.SQL: INFO
    org.example.service: INFO
  file:
    name: /app/logs/application.log
    max-size: 100MB
    max-history: 30

